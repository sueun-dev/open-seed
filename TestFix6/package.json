{
  "name": "top-down-shooter",
  "version": "1.0.0",
  "description": "Minimal top-down shooter browser game",
  "scripts": {
    "start": "npx serve -l 3000",
    "build": "echo 'Static site — no build step required' && test -f index.html",
    "test": "npx playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "serve": "^14.2.0"
  }
}
