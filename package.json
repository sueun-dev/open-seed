{
  "name": "agent40",
  "version": "0.1.0",
  "description": "Terminal-first 40-role coding agent MVP",
  "type": "module",
  "bin": {
    "agent": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --import tsx ./src/cli.ts",
    "test": "vitest run",
    "test:e2e": "vitest run tests/cli-e2e.test.ts",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "playwright-core": "^1.54.0",
    "typescript": "^5.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "vitest": "^4.0.0"
  }
}
