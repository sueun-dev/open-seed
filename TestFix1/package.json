{
  "name": "snake-game",
  "version": "1.0.0",
  "description": "HTML5 Canvas 스네이크 게임",
  "main": "src/main.js",
  "scripts": {
    "start": "npx http-server . -p 3000 -o",
    "test": "jest --verbose",
    "build": "echo 'No build step needed for vanilla JS' && exit 0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "http-server": "^14.1.1"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}
